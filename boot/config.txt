# 应用设备树重载
#dtoverlay=
# 在默认情况下 蓝牙使用的是全速的uart,而调试用的是miniuart
# 删除此行注释符来切换
#dtoverlay=pi3-miniuart-bt

# 应用设备树参数
#dtparam=

# 开启调试uart
enable_uart=1

# 摄像头模块
start_x=1

# 显存/MiB
gpu_mem=128

# 第三引导程序 文件名必须为kernel[N].img 这里的kernel8.img文件是uboot-201904
kernel=kernel8.img

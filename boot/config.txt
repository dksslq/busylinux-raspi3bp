# 应用设备树重载
#dtoverlay=
# 在默认情况下 蓝牙使用的是全速的uart,而调试用的是miniuart
# 删除此行注释来进行交换
#dtoverlay=pi3-miniuart-bt

# 开启调试uart
enable_uart=1

# start_x Enable the camera module. 
start_x=1

# 显存128MiB
gpu_mem=128

# 用户空间代码(第三引导程序) 文件名必须为kernel*.img 这里的kernel8.img文件是uboot
kernel=kernel8.img
